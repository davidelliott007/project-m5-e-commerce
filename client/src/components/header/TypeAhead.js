import React from "react";
import styled from "styled-component";

const TypeAhead = ({ isOpened }) => {
  return (
    <Wrapper>
      <p>This is a test</p>
    </Wrapper>
  );
};

const Wrapper = styled.div``;
